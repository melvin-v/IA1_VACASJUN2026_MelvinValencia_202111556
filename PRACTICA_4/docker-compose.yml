services:
  backend:
    build: ./backend
    container_name: robomaze-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build: ./frontend
    container_name: robomaze-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped

# La interfaz queda disponible en http://localhost:8080
# La API (y su Swagger en /docs) en http://localhost:8000
